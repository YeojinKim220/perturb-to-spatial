# Work Log — 2026-07-09

## Environment / Context
- Host: PACE-Phoenix. Only light reconnaissance performed on the login node (metadata-only reads of .var/.obs via h5py, matrices not loaded).
- Env used: env_SpatialDE (h5py availability confirmed). No downloads — remote files queried directly.

## Attempts

### [2026-07-09] Data reconnaissance + spatial platform decision (Day 1-2 blocker)
- **Tried:** Read the gene/cell-type/niche structure of DE_stats.h5ad and CosMx NSCLC clustered.h5ad,
  and computed the number of shared genes (gene overlap). For resolving proposal §7 open question #4 (alignment stability).
- **Result (success):**
  - Perturb-seq DE_stats: 10,282 measured genes / 11,526 perturbed genes; conditions Rest·Stim8hr·Stim48hr confirmed.
  - CosMx NSCLC: 960-plex panel, 765,771 cells, 8 samples (LUAD-5 R1-3, LUSC-6, LUAD-9 R1-2, LUAD-12, LUAD-13).
  - CosMx .obs pre-annotations obtained: cell_type (22 types, including T CD4 memory/naive·Treg·CD8·tumor·stroma, etc.),
    niche (9 types: immune, lymphoid structure (=TLS-like), tumor-stroma boundary, etc.), sample/patient/fov.
  - **Shared genes = 501** (out of the 960-panel). Exceeds the "few hundred" lower bound the proposal was concerned about → usable for alignment computation.
- **Failed:** N/A
- **Next:** Begin Step 1 — extract spatially organized T-cell programs from CosMx using Moran's I.
  Alignment should always be computed only over the 501 shared genes, and the overlap size should be reported with every score.

**Verdict:** CosMx NSCLC can be used as the primary spatial dataset. No need to additionally acquire Visium HD.
Basis: (1) overlap of 501 is sufficient, (2) CD4 subpopulations (memory/naive/Treg) are present in the panel, directly linking to CD4+ Perturb-seq, (3) the 'lymphoid structure' niche is an ideal positive control for organized T-cell programs, (4) single-cell resolution.