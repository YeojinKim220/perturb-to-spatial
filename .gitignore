# Data — never commit the large public datasets; download via step0_data/download_data.sh
0-Data/
*.h5ad
*.h5mu
*.rds
*.zip

# Results / figures / logs (regenerate by running the steps)
2-Results/
res/
fig/
log/
*.png
*.pdf
!report/figures/*.png

# Node / Remotion (video project) — commit source + the rendered mp4, not deps
node_modules/
.remotion/
video/out/*
!video/out/report.mp4
!video/public/*.png

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
env/

# macOS
.DS_Store
